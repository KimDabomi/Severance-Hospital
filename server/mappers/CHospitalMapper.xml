<!--
File Name: CHospitalMapper.xml
Author: 박다윗 (davidpark.0098@gmail.com)
Last Update: 2023-01-09 00:33:33
Description: 협력병원 맵퍼
-->

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CHospitalMapper">
    <!-- 단일행 조회를 위한 기능 정의 -->
    <select id="selectItem">
        SELECT id, CHospitalArea, CHospitalIntroduction, CHospitalAddress, CHospitalZipCode, CHospitalTel, CHospitalName, CMedicalDepartment, CHospitalURL, regDate, editDate FROM CHospital WHERE id=#{id}
    </select>
    <!-- 다중행 조회를 위한 기능 정의 -->
    <select id="selectList">
        SELECT id, CHospitalArea, CHospitalIntroduction, CHospitalAddress, CHospitalZipCode, CHospitalTel, CHospitalName, CMedicalDepartment, CHospitalURL, regDate, editDate FROM CHospital

        <where>
            <if test="CHospitalArea != null and CHospitalArea != ''">
                CHospitalArea LIKE concat('%', #{CHospitalArea}, '%')
            </if>
            <if test="CHospitalName != null and CHospitalName != ''">
                OR CHospitalName LIKE concat('%', #{CHospitalName}, '%')
            </if>
        </where>

        ORDER BY id DESC

        <!-- 페이지 구현을 위한 LIMIT절 추가 -->
        <if test="listCount > 0">
            LIMIT ${offset}, ${listCount}
        </if>
    </select>


    <!-- 데이터 저장을 위한 기능 정의 -->
    <insert id="insertItem">
        INSERT INTO CHospital (CHospitalArea, CHospitalIntroduction, CHospitalAddress, CHospitalZipCode, CHospitalTel, CHospitalName, CMedicalDepartment, CHospitalURL, regDate) VALUES (#{CHospitalArea}, #{CHospitalIntroduction}, #{CHospitalAddress}, #{CHospitalZipCode}, #{CHospitalTel}, #{CHospitalName}, #{CMedicalDepartment}, #{CHospitalURL}, now())
    </insert>


    <!-- 데이터 삭제를 위한 기능 정의 -->
    <delete id="deleteItem">
        DELETE FROM CHospital WHERE id=#{id}
    </delete>


    <!-- 데이터 갱신을 위한 기능 정의 -->
    <update id="updateItem">
        UPDATE CHospital SET CHospitalArea=#{CHospitalArea}, CHospitalIntroduction=#{CHospitalIntroduction}, CHospitalAddress=#{CHospitalAddress}, CHospitalZipCode=#{CHospitalZipCode}, CHospitalTel=#{CHospitalTel}, CHospitalName=#{CHospitalName}, CMedicalDepartment=#{CMedicalDepartment}, CHospitalURL=#{CHospitalURL}, editDate=now()
        WHERE id=#{id}
    </update>


    <!-- 데이터 수 조회하기  -->
    <select id="selectCountAll">
        SELECT COUNT(*) AS cnt FROM CHospital

        <where>
            <if test="CHospitalName != null and CHospitalName != ''">
                CHospitalName LIKE concat('%', #{CHospitalName}, '%')
            </if>
            <if test="CHospitalArea != null and CHospitalArea != ''">
                OR CHospitalArea LIKE concat('%', #{CHospitalArea}, '%')
            </if>
        </where>
    </select>
</mapper>
